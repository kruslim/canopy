"""L4/L5 — Agent orchestration & structured outputs (above the seam). Empty until Phase 3.

Nothing here may name the data source or import ``readers/`` concretions directly;
data is reached only through the ``SignalReader`` protocol and ``domain/``
(docs/02, enforced by ``tests/test_seam.py``). No LLM code until Phase 3.
"""
