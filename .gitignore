# ── IP hygiene (see docs/00 and docs/02) — NEVER commit proprietary artifacts ──
data/captures/proprietary/
*.private.dbc

# ── Secrets — API keys live in .env, never in the tree ──
.env

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# ── OS ──
.DS_Store

# Local Claude Code preview/launch config
.claude/
